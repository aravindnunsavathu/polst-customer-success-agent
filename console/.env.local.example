API_URL=http://localhost:8000
# NEXT_PUBLIC_ prefix required — the approval queue page POSTs directly
# from the browser (approve/reject are user actions, not page loads), so
# this one needs to be visible in the client bundle.
NEXT_PUBLIC_API_URL=http://localhost:8000
